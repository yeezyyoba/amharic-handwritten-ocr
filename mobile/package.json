{
  "name": "amharic-ocr-mobile",
  "version": "0.0.1",
  "private": true,
  "description": "PLACEHOLDER — this file documents intended dependencies. Replace with the real package.json generated by `npx create-expo-app` or `npx react-native init` once the RN tooling choice is finalized.",
  "dependencies": {
    "react": "^18.2.0",
    "react-native": "^0.74.0"
  },
  "notes": "Add camera/image-picker and HTTP client libraries (e.g. expo-image-picker or react-native-image-picker, and axios) once the project is initialized."
}
